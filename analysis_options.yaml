include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    # Не допускаем неявные потери типовой информации на границах API и данных.
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

formatter:
  # Стандартная ширина даёт многострочным вызовам место для trailing comma.
  page_width: 80

linter:
  rules:
    # Устойчивые правила сверх flutter_lints: безопасность типов, понятный API
    # и предсказуемая Flutter-вёрстка без спорных стилистических предпочтений.
    always_declare_return_types: true
    always_put_required_named_parameters_first: true
    avoid_dynamic_calls: true
    avoid_print: true
    avoid_unnecessary_containers: true
    cancel_subscriptions: true
    directives_ordering: true
    prefer_final_locals: true
    require_trailing_commas: true
    sort_child_properties_last: true
    use_super_parameters: true
